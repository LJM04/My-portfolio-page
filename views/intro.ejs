<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= profile.name %> - 자기소개서</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="card shadow p-5">
            <div class="border-bottom pb-4 mb-4 text-center">
                <h1 class="fw-bold">자기소개서</h1>
                <p class="text-muted"><%= profile.school %> | <%= profile.major %> | <%= profile.name %></p>
            </div>
            
            <div class="content" style="white-space: pre-line; line-height: 1.8; font-size: 1.1rem;">
                <%= profile.intro_content %>
            </div>

            <div class="text-center mt-5">
                <a href="/" class="btn btn-dark px-4">홈으로 돌아가기</a>
            </div>
        </div>
    </div>
</body>
</html>