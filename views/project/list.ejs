<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Projects - OOO Portfolio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">임정민 Portfolio</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link active" href="/projects">Project</a>
                <a class="nav-link" href="/studies">Study</a>
            </div>
        </div>
    </nav>

    <div class="container main-container p-5 bg-white shadow rounded mb-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold">All Projects</h2>
            <% if (isLoggedIn) { %>
                <a href="/write/project" class="btn btn-primary">+ 프로젝트 추가</a>
            <% } %>
        </div>

        <div class="row">
            <% projects.forEach(function(project) { %>
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 shadow-sm hover-effect overflow-hidden">
                        
                        <% 
                            let imgList = project.img_path ? project.img_path.split(',') : [];
                        %>

                        <% if(imgList.length > 0) { %>
                            <div id="carousel-list-<%= project.id %>" class="carousel slide" data-bs-ride="false">
                                
                                <div class="carousel-inner">
                                    <% imgList.forEach(function(img, index) { %>
                                        <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                            <img src="<%= img.trim() %>" class="d-block w-100 project-card-img" alt="프로젝트 이미지">
                                        </div>
                                    <% }); %>
                                </div>

                                <% if(imgList.length > 1) { %>
                                    <button class="carousel-control-prev" type="button" data-bs-target="#carousel-list-<%= project.id %>" data-bs-slide="prev">
                                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                        <span class="visually-hidden">Previous</span>
                                    </button>
                                    <button class="carousel-control-next" type="button" data-bs-target="#carousel-list-<%= project.id %>" data-bs-slide="next">
                                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                        <span class="visually-hidden">Next</span>
                                    </button>
                                <% } %>
                            </div>
                        <% } else { %>
                            <div class="bg-secondary text-white d-flex align-items-center justify-content-center project-card-img">
                                <span>No Image</span>
                            </div>
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title fw-bold"><%= project.title %></h5>
                            <p class="card-text text-muted small text-truncate"><%= project.description %></p>
                            <div class="mb-3">
                                <% project.tags.forEach(function(tag) { %>
                                    <span class="badge bg-secondary"><%= tag %></span>
                                <% }); %>
                            </div>
                            
                            <a href="/project/<%= project.id %>" class="btn btn-outline-dark w-100">상세보기</a>

                            <% if (isLoggedIn) { %>
                                <div class="mt-2 text-end z-index-front">
                                    <a href="/edit/project/<%= project.id %>" class="text-secondary small me-2 text-decoration-none">수정</a>
                                    <a href="/delete/project/<%= project.id %>" class="text-danger small text-decoration-none" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }); %>
            
            <% if(projects.length === 0) { %>
                <div class="col-12 text-center py-5 text-muted">
                    등록된 프로젝트가 없습니다.
                </div>
            <% } %>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>