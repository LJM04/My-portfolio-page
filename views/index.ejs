<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profile.name %>의 포트폴리오</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/"><%= profile.name %> Portfolio</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#aboutModal">About</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="/projects">Project</a></li>
                    <li class="nav-item"><a class="nav-link" href="/studies">Study</a></li>
                    
                    <% if (isLoggedIn) { %>
                        <li class="nav-item ms-lg-2"><a class="nav-link text-warning" href="/logout">로그아웃</a></li>
                    <% } else { %>
                        <li class="nav-item ms-lg-2"><a class="nav-link text-white-50" href="/login"><small>관리자</small></a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container main-container p-5 bg-white shadow rounded mb-5">
        
        <section class="text-center mb-5 p-5 bg-light rounded border position-relative">
            <h2 class="fw-bold mb-3">"개발자가 되기 위한 성장 기록 보관소"</h2>
            <p class="text-muted fs-5"><%= profile.school %> | <%= profile.major %></p>
            <p class="fs-4 fw-bold"><%= profile.name %></p>
            
            <div class="d-flex justify-content-center gap-3 mt-4">
                <% if(profile.github_url) { %>
                    <a href="<%= profile.github_url %>" target="_blank" class="btn btn-dark px-4"><i class="bi bi-github"></i> GitHub</a>
                <% } %>
                <a href="/intro" class="btn btn-outline-dark px-4">자기소개서 보기</a>
            </div>
            
            <% if (isLoggedIn) { %>
                <div class="position-absolute top-0 end-0 m-3">
                    <a href="/edit/profile" class="btn btn-sm btn-danger">내 정보 수정</a>
                </div>
            <% } %>
        </section>

        <div class="row h-100">
            
            <div class="col-lg-5 mb-4 mb-lg-0 pe-lg-4 border-end-lg" id="project">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3 class="fw-bold border-bottom border-3 border-dark pb-1">Recent Projects</h3>
                    <div class="d-flex gap-2">
                        <% if (isLoggedIn) { %>
                            <a href="/write/project" class="btn btn-sm btn-primary">+ 추가</a>
                        <% } %>
                        <a href="/projects" class="btn btn-sm btn-outline-dark">전체보기 ></a>
                    </div>
                </div>
                
                <% projects.forEach(function(project) { %>
                    <div class="card mb-3 shadow-sm hover-effect overflow-hidden">
                        
                        <% 
                            // DB에 콤마(,)로 구분되어 저장될 경우를 대비해 배열로 변환
                            let imgList = project.img_path ? project.img_path.split(',') : [];
                        %>

                        <% if(imgList.length > 0) { %>
                            <div id="carousel-<%= project.id %>" class="carousel slide" data-bs-ride="false"> <div class="carousel-inner">
                                    <% imgList.forEach(function(img, index) { %>
                                        <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                            <img src="<%= img.trim() %>" class="d-block w-100 project-card-img" alt="프로젝트 이미지">
                                        </div>
                                    <% }); %>
                                </div>

                                <% if(imgList.length > 1) { %>
                                    <button class="carousel-control-prev" type="button" data-bs-target="#carousel-<%= project.id %>" data-bs-slide="prev">
                                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                        <span class="visually-hidden">Previous</span>
                                    </button>
                                    <button class="carousel-control-next" type="button" data-bs-target="#carousel-<%= project.id %>" data-bs-slide="next">
                                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                        <span class="visually-hidden">Next</span>
                                    </button>
                                <% } %>
                            </div>
                        <% } else { %>
                            <div class="bg-secondary text-white d-flex align-items-center justify-content-center project-card-img">
                                <span>No Image</span>
                            </div>
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title fw-bold"><%= project.title %></h5>
                            <p class="card-text text-muted small text-truncate"><%= project.description %></p>
                            <div class="d-flex gap-1 mb-2">
                                <% project.tags.forEach(function(tag) { %>
                                    <span class="badge bg-secondary"><%= tag %></span>
                                <% }); %>
                            </div>
                            
                            <a href="/project/<%= project.id %>" class="btn btn-sm btn-outline-dark w-100 mt-2">상세보기</a>
                            
                            <% if (isLoggedIn) { %>
                                <div class="mt-2 text-end z-index-front">
                                    <a href="/edit/project/<%= project.id %>" class="text-secondary small me-2 text-decoration-none">수정</a>
                                    <a href="/delete/project/<%= project.id %>" class="text-danger small text-decoration-none" onclick="return confirm('삭제?');">삭제</a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
                
                <% if(projects.length === 0) { %>
                    <p class="text-center text-muted py-5">등록된 프로젝트가 없습니다.</p>
                <% } %>
            </div>

            <div class="col-lg-7 ps-lg-4 d-flex flex-column" id="study">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3 class="fw-bold border-bottom border-3 border-dark pb-1">Study Log</h3>
                    <div class="d-flex gap-2">
                        <% if (isLoggedIn) { %>
                            <a href="/write/study" class="btn btn-sm btn-primary">+ 글쓰기</a>
                        <% } %>
                        <a href="/studies" class="btn btn-sm btn-outline-dark">전체보기 ></a>
                    </div>
                </div>

                <div class="accordion mb-auto" id="simpleStudyAccordion">
                    <% 
                        const grades = Object.keys(simpleStudyTree).sort();
                    %>
                    <% if(grades.length > 0) { %>
                        <% grades.forEach(function(year, index) { %>
                            <div class="accordion-item">
                                <h2 class="accordion-header">
                                    <button class="accordion-button collapsed fw-bold" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= index %>">
                                        📂 <%= year %>
                                    </button>
                                </h2>
                                <div id="collapse<%= index %>" class="accordion-collapse collapse" data-bs-parent="#simpleStudyAccordion">
                                    <div class="accordion-body bg-light">
                                        <div class="d-flex flex-wrap gap-2">
                                            <% Array.from(simpleStudyTree[year]).sort().forEach(function(subject) { %>
                                                <a href="/studies" class="btn btn-white border shadow-sm text-dark text-decoration-none">
                                                    📖 <%= subject %>
                                                </a>
                                            <% }); %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                         <div class="alert alert-light text-center border">아직 등록된 스터디가 없습니다.</div>
                    <% } %>
                </div>
                
                <div class="alert alert-secondary mt-4 text-center p-4">
                    <i class="bi bi-book fs-1"></i>
                    <p class="mt-2 mb-0 small">
                        과목을 클릭하면 <strong>전체 스터디 목록 페이지</strong>로 이동하여<br>
                        상세 내용을 확인할 수 있습니다.
                    </p>
                </div>
            </div>
        </div>
    </div>
    
    <footer class="text-center py-4 bg-dark text-white mt-auto">
        <p class="mb-0">Copyright © 2025 <%= profile.name %> Portfolio</p>
    </footer>

    <div class="modal fade" id="aboutModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-dark text-white">
                    <h5 class="modal-title fw-bold">About Me</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="text-center mb-4">
                        <div style="width: 80px; height: 80px; background-color: #eee; border-radius: 50%; margin: 0 auto; display: flex; align-items: center; justify-content: center;">
                            <i class="bi bi-person-fill fs-1 text-secondary"></i>
                        </div>
                        <h4 class="fw-bold mt-3"><%= profile.name %></h4>
                        <p class="text-muted"><%= profile.major %></p>
                    </div>
                    <ul class="list-group list-group-flush mb-3">
                        <li class="list-group-item"><strong>🎓 학교:</strong> <%= profile.school %></li>
                        <li class="list-group-item"><strong>📚 학과:</strong> <%= profile.major %></li>
                        <li class="list-group-item"><strong>📧 Email:</strong> <%= profile.email %></li>
                        <li class="list-group-item"><strong>🏠 주소:</strong> <%= profile.address %></li>
                        <li class="list-group-item"><strong>💻 Skills:</strong> <%= profile.skills %></li>
                    </ul>
                </div>
                <div class="modal-footer">
                    <a href="/intro" class="btn btn-outline-dark w-100">자기소개서 전체 보기</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>