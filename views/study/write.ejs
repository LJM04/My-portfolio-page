<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8" />
        <title>스터디 등록</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
    </head>
    <body class="bg-light">
        <div class="container mt-5" style="max-width: 600px">
            <div class="card shadow p-4">
                <h3 class="fw-bold mb-4">스터디 / 강의 정리 등록</h3>
                <form
                    action="/write/study"
                    method="POST"
                    enctype="multipart/form-data"
                >
                    <div class="row mb-3">
                        <div class="col">
                            <label class="form-label">학년</label>
                            <select name="grade" class="form-select">
                                <option value="1학년">1학년</option>
                                <option value="2학년">2학년</option>
                                <option value="3학년">3학년</option>
                            </select>
                        </div>
                        <div class="col">
                            <label class="form-label">과목명</label>
                            <input
                                type="text"
                                name="subject"
                                class="form-control"
                                placeholder="예: 자료구조"
                                required
                            />
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">글 제목</label>
                        <input
                            type="text"
                            name="title"
                            class="form-control"
                            placeholder="예: 배열과 리스트의 차이"
                            required
                        />
                    </div>
                    <div class="mb-3">
                        <label class="form-label"
                            >첨부 파일 (PDF, PPT 등)</label
                        >
                        <input
                            type="file"
                            name="study_file"
                            class="form-control"
                        />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">내용 정리</label>
                        <textarea
                            name="content"
                            class="form-control"
                            rows="5"
                        ></textarea>
                    </div>
                    <button type="submit" class="btn btn-dark w-100">
                        등록하기
                    </button>
                </form>
            </div>
        </div>
    </body>
</html>
