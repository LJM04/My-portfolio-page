<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>스터디 내용 수정</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow p-4">
            <h3 class="fw-bold mb-4">스터디 기록 수정</h3>
            <form action="/edit/study/<%= study.id %>" method="POST" enctype="multipart/form-data">
                <div class="row mb-3">
                    <div class="col">
                        <label class="form-label">학년</label>
                        <select name="grade" class="form-select">
                            <option value="1학년" <%= study.grade === '1학년' ? 'selected' : '' %>>1학년</option>
                            <option value="2학년" <%= study.grade === '2학년' ? 'selected' : '' %>>2학년</option>
                            <option value="3학년" <%= study.grade === '3학년' ? 'selected' : '' %>>3학년</option>
                        </select>
                    </div>
                    <div class="col">
                        <label class="form-label">과목명</label>
                        <input type="text" name="subject" class="form-control" value="<%= study.subject %>" required>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label">글 제목</label>
                    <input type="text" name="title" class="form-control" value="<%= study.title %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">첨부 파일 (변경 시 선택)</label>
                    <input type="file" name="study_file" class="form-control">
                    <% if(study.origin_filename) { %>
                        <p class="small text-muted mt-1">현재 파일: <%= study.origin_filename %></p>
                    <% } %>
                </div>
                <div class="mb-3">
                    <label class="form-label">내용 정리</label>
                    <textarea name="content" class="form-control" rows="5"><%= study.content %></textarea>
                </div>
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-dark">수정 완료</button>
                    <a href="/studies" class="btn btn-outline-secondary">취소</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>